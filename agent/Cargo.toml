[package]
name = "agent"
version = "0.1.0"
edition = "2024"

[dependencies]
common = { path = "../common" }
serde = { version = "1.0", features = ["derive"] }
whoami = "1.5"
tokio = { version = "1.28", features = ["full"] }
socket2 = { version = "0.5", features = ["all"] }
